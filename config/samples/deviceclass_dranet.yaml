# Basic DeviceClass that matches every DRANET-published device.
# NumaNetwork.spec.refDeviceClass.name must reference this object.
apiVersion: resource.k8s.io/v1
kind: DeviceClass
metadata:
  name: dranet
spec:
  selectors:
    - cel:
        expression: device.driver == "dra.net"
