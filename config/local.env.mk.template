# Local environment configuration for gpu-direct-comm.
# Copy this file to config/local.env.mk and fill in your values.
#
#   cp config/local.env.mk.template config/local.env.mk
#
# config/local.env.mk is gitignored and will not be committed.

# Container registry address for your environment.
# Examples:
#   Baremetal:        IMG = <registry-host>:<port>/<project>/controller:latest
IMG = controller:latest
WEBHOOK_NN_IMG = webhook-whereabouts-numanetwork:latest
