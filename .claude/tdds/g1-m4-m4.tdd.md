# TDD Report: G1-M4 — Pipeline Vertex への ResourceClaim 自動注入

**Plan**: `.claude/plans/g1-m4.plan.md`
**Branch**: `feature/g1-m4-pipeline-vertex-resourceclaim`
**Date**: 2026-06-29

---

## 1. User Journeys

1. **Pipeline オペレータとして**、`gpu-direct-comm.numaproj.io/numa-network-edges` annotation を Pipeline に付与するだけで、対象 vertex の `spec.resourceClaims` に RCT 参照が自動注入される。これにより numaflow の既存コードを改変せずに DRA による Secondary NIC 割り当てが有効になる。

2. **既存 Pipeline オペレータとして**、annotation を付与していない Pipeline は従来通り変更なく作成・更新でき、Mutating Webhook による意図しない変更を受けない。

---

## 2. Task Report

### Task 1: Mutating Webhook ハンドラの実装

**Testing Policy**  
`PipelineMutator.Handle` の正確性を保証するために、以下の側面を unit test で検証する必要があった：
- annotation 不在の場合の素通り（既存 Pipeline への非破壊性）
- `connectionType: direct` の束縛で `from`/`to` 両 vertex への注入
- 同一 vertex が複数束縛に現れる場合の重複排除
- 既存 `resourceClaims` がある vertex への追記（既存エントリ消去の防止）
- `connectionType: direct` 以外の束縛のスキップ

これら 5 ケースは ADR-0009 の設計仕様に直接対応しており、実装のコーナーケースを網羅する。

**Execution Log**  
`internal/webhook/v1alpha1/pipeline_mutator.go` を実装し `go build ./internal/webhook/...` で検証した。

```
go build ./internal/webhook/...
# 出力なし（成功）
```

**Test Results**

| テスト関数 | 検証内容 | 種別 | 結果 |
|---|---|---|---|
| [`TestPipelineMutator_Handle/(a)`](../../../internal/webhook/v1alpha1/pipeline_mutator_test.go) | annotation 不在時は `Allowed` かつパッチなし | Unit | PASS |
| [`TestPipelineMutator_Handle/(b)`](../../../internal/webhook/v1alpha1/pipeline_mutator_test.go) | 有効な direct 束縛で from/to 両 vertex に RCT 参照が注入される | Unit | PASS |
| [`TestPipelineMutator_Handle/(c)`](../../../internal/webhook/v1alpha1/pipeline_mutator_test.go) | 複数束縛で同一 vertex が共有される場合に重複排除される | Unit | PASS |
| [`TestPipelineMutator_Handle/(d)`](../../../internal/webhook/v1alpha1/pipeline_mutator_test.go) | 既存 resourceClaims のある vertex に追記し、既存エントリを消去しない | Unit | PASS |
| [`TestPipelineMutator_Handle/(e)`](../../../internal/webhook/v1alpha1/pipeline_mutator_test.go) | `connectionType` が `direct` でない束縛はスキップされパッチなし | Unit | PASS |

**保証**: 5 つのテストにより、`PipelineMutator` は仕様通りに動作する。annotation なし Pipeline を素通りさせ、`direct` 束縛の両端 vertex に RCT 参照を重複なく注入し、既存フィールドを破壊しないことが保証される。

---

### Task 2: ユニットテスト（RED → GREEN）

**Execution Log**  
`pipeline_mutator_test.go` を先に書き（RED 確認）、スタブ実装でコンパイル・実行し、ケース b/c/d が失敗することを確認した。その後フル実装で全 5 ケース PASS（GREEN）に到達した。

```
go test -race ./internal/webhook/... -run TestPipelineMutator
ok  	github.com/numaproj-contrib/gpu-direct-comm/internal/webhook/v1alpha1	0.191s
```

---

### Task 3: main.go への Mutating Webhook 登録

**Execution Log**  
`cmd/main.go` に `PipelineMutator` のハンドラを Mutating Webhook パス `/mutate-numaflow-numaproj-io-v1alpha1-pipeline` で登録し、`go build ./...` で検証した。

```
go build ./...
# 出力なし（成功）
```

---

### Task 4: Webhook 構成生成（make manifests）

**Execution Log**  
`make manifests` を実行し、`+kubebuilder:webhook` マーカーから `MutatingWebhookConfiguration` が生成されたことを確認した。

```
grep -q "MutatingWebhookConfiguration" config/webhook/manifests.yaml && \
grep -q "ValidatingWebhookConfiguration" config/webhook/manifests.yaml && \
echo "BOTH FOUND"
BOTH FOUND
```

`make build-installer` も成功し、kustomize overlay の整合性を確認した。

さらに `config/default/kustomization.yaml` の cert-manager CA 注入設定（コメントアウト部分）を有効化し、MutatingWebhookConfiguration にも `cert-manager.io/inject-ca-from` annotation が設定されるようにした。

---

### Task 5: ビルド・生成物・全体テスト確認

**Execution Log**  
全パッケージのテストが PASS であることを確認した。

```
go test -race ./... | grep -E "^(ok|FAIL)"
ok  	github.com/numaproj-contrib/gpu-direct-comm/api/v1alpha1
ok  	github.com/numaproj-contrib/gpu-direct-comm/internal/controller
ok  	github.com/numaproj-contrib/gpu-direct-comm/internal/webhook/v1alpha1
```

---

### Task 6: k3d で実 admission 検証

**Execution Log**  
k3d クラスタ（`numaflow-cluster`）上で以下を検証した：

1. annotation 付き Pipeline を apply
2. `kubectl get pipeline` で `spec.vertices[*].resourceClaims` を確認

```
kubectl get pipeline test-injection-pipeline \
  -o jsonpath='{.spec.vertices[*].resourceClaims}'

[{"name":"pipeline1-multi-network-rct","resourceClaimTemplateName":"pipeline1-multi-network-rct"}] [{"name":"pipeline1-multi-network-rct","resourceClaimTemplateName":"pipeline1-multi-network-rct"}]
```

`filter-resize` と `inference` の両 vertex に `pipeline1-multi-network-rct` が注入された。

annotation なし Pipeline のリソースを確認：

```
kubectl get pipeline test-no-annotation-pipeline \
  -o jsonpath='{.spec.vertices[*].resourceClaims}'
# 出力なし（空）
```

annotation のない Pipeline には `resourceClaims` が注入されず、非破壊性が確認された。

---

## 3. Coverage

```
go test -cover ./internal/webhook/...
ok  github.com/numaproj-contrib/gpu-direct-comm/internal/webhook/v1alpha1  coverage: 88.1% of statements
```

**Known Gaps**: `cmd/main.go` は Go バージョン不一致（1.25.7 vs 1.25.3）により `-cover` が使用不可。webhook パッケージ単体では 88.1% で 80% の閾値を超えている。
